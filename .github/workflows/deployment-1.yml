name: Deployment Exercise 1
on: push
jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Get code
              uses: action/checkout@v3
            - name: Install Dependencies
              run: npm ci
            - name: lint
              run: npm run lint
            - name: test code
              run: npm run test
            - name: Build code
              run: npm run build
            - name: Deploy code
              run: echo"Deploying ...."